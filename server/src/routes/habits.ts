import Router from 'express';
import { Habit } from '../models/Habit';

export const habitRoute = Router();

habitRoute.post('/createhabit', async (req, res) => {
    try{

    }
    catch(e) {
        console.log(e);
    }
});